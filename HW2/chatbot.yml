version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  chatbot:
    lang: python
    handler: ./chatbot
    image: krdindorkar/chatbot:latest
    environment:
      read_timeout: 60
      write_timeout: 60
      dependencies: ./requirements.txt

